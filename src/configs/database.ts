const DATA_BASE_NAME ='@gameplay';

//@app: collection
const COLLECT_USERS = `${DATA_BASE_NAME}:user`;
const COLLECT_APPOINTMENTS = `${DATA_BASE_NAME}:appointments`;

export {
    COLLECT_USERS,
    COLLECT_APPOINTMENTS
}